[project]
name = "mft"
version = "0.1.0"
description = "Mid-frequency algo trading platform"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/mft_protocol",
    "packages/mft_broker",
    "services/api",
    "services/adapter",
    "services/market_data",
    "services/trading",
    "services/strategy",
]

[tool.uv]
package = false
